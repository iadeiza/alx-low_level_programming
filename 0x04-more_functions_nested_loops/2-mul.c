#include <stdio.h>
#include "main.h"

/**
 * mul - multiply two numbers,'a' and 'b'
 * @a: first integer
 * @b: second integer
 * Return: Always 0
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
