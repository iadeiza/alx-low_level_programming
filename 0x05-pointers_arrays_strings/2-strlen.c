#include "main.h"

/**
 * _strlen - Calculates the length of a string
 * @str: pointer varable
 * Return: Returns length
 */

int _strlen(char *s)
{
	int i = 0;

	for (i = 0; s[i] != '\0'; i++)
	{

	}
	return (i);
}

